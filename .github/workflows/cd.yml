name: CD - Terraform Infra + ArgoCD GitOps on AWS EKS

on:
  push:
    branches: [main]
    paths:
      - "terraform/**"
      - "helm/**"
      - "argo/**"
      - "k8s/**"
  workflow_dispatch:
    inputs:
      action:
        description: "Terraform action"
        required: true
        default: "apply"
        type: choice
        options: [plan, apply, destroy]

env:
  AWS_REGION:       ap-south-1
  TF_DIR:           terraform
  EKS_CLUSTER_NAME: ecommerce-eks
  GITHUB_REPO:      https://github.com/parthorookie/ecommercethreetierevent1editedcopyreviewpracticelive

jobs:

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 1 — Terraform Plan
  # ═══════════════════════════════════════════════════════════════════════════
  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.10.0"

      - name: Terraform Init & Plan
        working-directory: ${{ env.TF_DIR }}
        env:
          DB_PASSWORD:     ${{ secrets.DB_PASSWORD }}
          OPERATOR_IP:     ${{ secrets.OPERATOR_IP_CIDR }}
        run: |
          terraform init -reconfigure

          echo "=== Generating Fresh Terraform Plan ==="
          terraform plan \
            -var="region=${{ env.AWS_REGION }}" \
            -var="db_password=${DB_PASSWORD}" \
            -var="project_name=ecommerce" \
            -var="environment=prod" \
            -var="operator_ip_cidr=${OPERATOR_IP}" \
            -out=tfplan

      - name: Upload plan artifact
        uses: actions/upload-artifact@v4
        with:
          name: tfplan
          path: ${{ env.TF_DIR }}/tfplan
          retention-days: 1

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 2 — Terraform Apply (Fresh Plan + Apply)
  # ═══════════════════════════════════════════════════════════════════════════
  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: terraform-plan
    if: github.ref == 'refs/heads/main' && (github.event.inputs.action == 'apply' || github.event_name == 'push')
    environment: production
    outputs:
      eks_cluster_name:   ${{ steps.tf_out.outputs.eks_cluster_name }}
      rabbitmq_ip:        ${{ steps.tf_out.outputs.rabbitmq_ip }}
      rabbitmq_public_ip: ${{ steps.tf_out.outputs.rabbitmq_public_ip }}
      aurora_endpoint:    ${{ steps.tf_out.outputs.aurora_endpoint }}
      alb_dns_name:       ${{ steps.tf_out.outputs.alb_dns_name }}
      ecr_backend_url:    ${{ steps.tf_out.outputs.ecr_backend_url }}
      ecr_worker_url:     ${{ steps.tf_out.outputs.ecr_worker_url }}
      aws_account_id:     ${{ steps.tf_out.outputs.aws_account_id }}
      vpc_id:             ${{ steps.tf_out.outputs.vpc_id }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.10.0"

      - name: Terraform Init
        working-directory: ${{ env.TF_DIR }}
        run: terraform init -reconfigure

      - name: Download plan artifact
        uses: actions/download-artifact@v4
        with:
          name: tfplan
          path: ${{ env.TF_DIR }}

      - name: Apply Terraform (using fresh plan logic)
        working-directory: ${{ env.TF_DIR }}
        env:
          DB_PASSWORD:     ${{ secrets.DB_PASSWORD }}
          OPERATOR_IP:     ${{ secrets.OPERATOR_IP_CIDR }}
        run: |
          echo "=== Generating Fresh Plan before Apply (to avoid stale plan error) ==="
          terraform plan \
            -var="region=${{ env.AWS_REGION }}" \
            -var="db_password=${DB_PASSWORD}" \
            -var="project_name=ecommerce" \
            -var="environment=prod" \
            -var="operator_ip_cidr=${OPERATOR_IP}" \
            -out=tfplan

          echo "=== Applying Terraform Changes ==="
          terraform apply -auto-approve tfplan

      - name: Capture Terraform outputs
        id: tf_out
        working-directory: ${{ env.TF_DIR }}
        run: |
          echo "eks_cluster_name=$(terraform output -raw eks_cluster_name 2>/dev/null || echo '')" >> $GITHUB_OUTPUT
          echo "rabbitmq_ip=$(terraform output -raw rabbitmq_private_ip 2>/dev/null || echo '')" >> $GITHUB_OUTPUT
          echo "rabbitmq_public_ip=$(terraform output -raw rabbitmq_public_ip 2>/dev/null || echo '')" >> $GITHUB_OUTPUT
          echo "aurora_endpoint=$(terraform output -raw aurora_endpoint 2>/dev/null || echo '')" >> $GITHUB_OUTPUT
          echo "alb_dns_name=$(terraform output -raw alb_dns_name 2>/dev/null || echo '')" >> $GITHUB_OUTPUT
          echo "ecr_backend_url=$(terraform output -raw ecr_backend_url 2>/dev/null || echo '')" >> $GITHUB_OUTPUT
          echo "ecr_worker_url=$(terraform output -raw ecr_worker_url 2>/dev/null || echo '')" >> $GITHUB_OUTPUT
          echo "aws_account_id=$(aws sts get-caller-identity --query Account --output text)" >> $GITHUB_OUTPUT
          echo "vpc_id=$(terraform output -raw vpc_id 2>/dev/null || echo '')" >> $GITHUB_OUTPUT

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 3–6 remain unchanged (install-cluster-tools, configure-argocd, etc.)
  # ═══════════════════════════════════════════════════════════════════════════

  # [Paste your original JOB 3, 4, 5, and 6 here without any changes]
  # They start from "install-cluster-tools" and use needs: terraform-apply correctly.
